---

- name: "Build docker images"
  hosts: localhost
  gather_facts: no
  tasks:
    - name: "Build centos-httpd image"
      docker_image:
        name: "centos-httpd"
        source: build
        build:
          path: "centos-httpd"
          pull: yes
