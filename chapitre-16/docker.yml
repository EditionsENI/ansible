apache:
  hosts:
    "apache[1:4]":
      container_image: "docker.io/redhat/ubi8"
      start_cmd: "{{ omit }}"

mysql:
  hosts:
    mysql1: {}

haproxy:
  hosts:
    haproxy1: {}
  vars:
    container_image: "docker.io/jrei/systemd-ubuntu"
    privileged: yes

all:
  vars:
    network_name: test.eni
    ansible_connection: docker
